{
  "name": "markit",
  "version": "1.0.0",
  "main": "app.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "build": "mkdir -p dist && cp -R markit/* dist/",
    "start": "npm run build && electron dist/src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "keywords": [],
  "dependencies": {
    "electron": "^29.0.0",
    "marked": "^12.0.0"
  },
  "description": ""
}
