{
  "name": "MarkIt",
  "version": "1.0.0",
  "main": "./markit/src/app.js",
  "scripts": {
    "format": "prettier --write markit/",
    "build": "mkdir -p dist && cp -R markit/* dist/",
    "start": "npm run build && electron dist/main/app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "npx electron-packager .  markit --all --icon markit/assets/images/markit.icns",
    "clear": "rm -rf dist markit-*"
  },
  "author": "",
  "license": "ISC",
  "keywords": [],
  "dependencies": {
    "@electron/remote": "^2.1.2",
    "@octokit/rest": "^20.0.2",
    "marked": "^12.0.0",
    "marked-base-url": "^1.1.3",
    "marked-code-preview": "^1.3.6",
    "marked-emoji": "^1.3.2"
  },
  "description": "Markdown Editor",
  "devDependencies": {
    "@electron/packager": "^18.1.3",
    "electron": "^29.0.1",
    "electron-reload": "^2.0.0-alpha.1",
    "prettier": "3.2.5"
  }
}
